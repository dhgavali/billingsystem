import 'package:flutter/material.dart';

class OldTransactions extends StatelessWidget {
  const OldTransactions({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Past Transactions"),
      ),
    );
  }
}
